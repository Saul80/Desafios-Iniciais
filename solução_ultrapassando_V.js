let R = parseInt(gets());

  let V = 0;

  let adicao = 0;
  let contagem = 0;

    while(V <= R) {
 
     V = parseInt(gets());

  }

    while(adicao <= V) {
  
    adicao += R;
    R++;
    contagem++;

  }

    console.log(contagem);
